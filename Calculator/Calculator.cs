namespace TestProjectCalculator;

public class Calculator
{
    public double Add(double l, double r) => l + r;
    public double Subtract(double l, double r) => l - r;
    public double Multiply(double l, double r) => l * r;
    public double Divide(double l, double r) => l / r;
}